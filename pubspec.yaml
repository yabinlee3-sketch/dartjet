name: dart_jet
description: "DartJet: Dart/Flutter 实现的快速开发框架，以 Android XJet 为架构母本（MVVM 四态 + Provider/SPI）"
version: 0.1.0
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

